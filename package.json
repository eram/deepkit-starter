{
  "type": "module",
  "scripts": {
    "app": "vite-node src/app.ts",
    "build": "docker build --build-arg \"BUILD_TS=$(node -e 'console.log(new Date().toISOString());')\" --target prod -t dk-test:latest . && docker build --build-arg \"BUILD_TS=$(node -e 'console.log(new Date().toISOString());')\" --target scan . && docker build --build-arg \"BUILD_TS=$(node -e 'console.log(new Date().toISOString());')\" --target logs --output type=local,dest=./build-logs .",
    "build:grype": "docker build --build-arg \"BUILD_TS=$(node -e 'console.log(new Date().toISOString());')\" --target scan .",
    "ci": "vite-node scripts/run-ci-tests.js",
    "dev": "vite-node --watch src/app.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "release": "node scripts/release.js",
    "test": "npm run lint && node scripts/run-vitest.js run",
    "test:unit": "node scripts/run-vitest.js run --coverage",
    "test:watch": "node scripts/run-vitest.js"
  },
  "dependencies": {
    "@deepkit/app": "^1.0.19",
    "@deepkit/type": "^1.0.19",
    "@deepkit/type-compiler": "^1.0.19",
    "@deepkit/vite": "^1.0.19",
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vite-node": "^3.2.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4"
  }
}
